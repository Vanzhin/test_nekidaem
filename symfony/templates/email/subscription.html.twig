<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ email.subject }}</title>
    <style>
        body {
            background-color: #99cdd8;
            font-family: sans-serif;
            font-size: 14px;
            line-height: 1.4;
        }

        .container {
            background-color: #b4b2b2;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            align-items: center;
        }

    </style>
</head>
<body>
<div class="container">
    <h1>Hello, {{ user.name }}</h1>
    <div>here is your weekly news</div>
    <ul>
    {% for post in posts %}
        <li><div>{{ post.title }} - {{ post.content }}</div></li>
    {% endfor %}
    </ul>
    <a href="{{ url('app_api_post_subscribed',{'user': user.id}) }}">View all</a>
</div>
</body>
</html>
